language: rust

addons:
  apt:
    packages:
      - librust-libdbus-sys-dev

rust:
  - stable

script:
  - cargo build --verbose --all
#  - cargo test --verbose --all

#jobs:
#  allow_failures:
#    - rust: stable
#  fast_finish: true
#
#cache: cargo